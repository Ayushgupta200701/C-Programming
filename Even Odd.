#include <stdio.h>
int main() {
    int N;
    // Taking input
    scanf("%d", &N);
    // Checking even or odd
    if (N % 2 == 0) {
        printf("Even");
    } else {
        printf("Odd");
    }
    return 0;
}
