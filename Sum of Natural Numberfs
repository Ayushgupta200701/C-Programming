#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    // Sum of first N natural numbers formula: N * (N + 1) / 2
    int sum = N * (N + 1) / 2;

    printf("%d\n", sum);

    return 0;
}
