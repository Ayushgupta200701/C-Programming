#include <stdio.h>

int main() {
    char ch;
    
    // Read a single character
    scanf("%c", &ch);
    
    // Check if the character is an alphabet (A-Z or a-z)
    if ((ch >= 'A' && ch <= 'Z') || (ch >= 'a' && ch <= 'z')) {
        printf("Alphabet\n");
    }
    // Check if the character is a digit (0-9)
    else if (ch >= '0' && ch <= '9') {
        printf("Digit\n");
    }
    // If neither alphabet nor digit, it is a symbol
    else {
        printf("Symbol\n");
    }
    
    return 0;
}
